import javax.swing.*;

public class AddCourse extends JPanel {
    public AddCourse() {
        // Placeholder content for the Add Course panel
        JLabel label = new JLabel("Add Course - Placeholder");
        add(label);
    }
}
